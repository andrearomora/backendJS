<h2>Products</h2>
<a href="/form-products">Create Products</a>
<hr>
<div>
    <h2>WELCOME!! <strong>{{user.first_name}} {{user.last_name}}</strong></h2>
    🍃
    <span><strong>Age: </strong></span>{{user.age}}
    🍃
    <span><strong>Email: </strong></span>{{user.email}}
    {{#if admin}}
        ⚡
        <strong>ROL: ADMIN</strong>
    {{/if}}
</div>
<table border="1">
    <thead>
        <td>Title</td>
        <td>Image</td>
        <td>Description</td>
        <td>Price</td>
        <td>Code</td>
        <td>Stock</td>
        <td>Category</td>
        <td>Add to cart</td>
    </thead>
    <tbody>
        {{#each docs}}
            <tr>
                <td>{{this.title}}</td>
                <td><img src="{{this.thumbnail}}" alt="" style="width: 100px;"></td>
                <td>{{this.description}}</td>
                <td>$ {{this.price}}</td>
                <td>{{this.code}}</td>
                <td>{{this.stock}}</td>
                <td>{{this.category}}</td>
                <td>
                    <button value="{{this._id}}" class="btn btn-primary" id = "addToCart">
                        <i class="fa-solid fa-cart-plus"></i>
                    </button>
                </td>
            </tr>
        {{/each}}
    </tbody>
</table>
{{#if hasPrevPage}}
    <button><a href="{{prevLink}}">PREVIOUS</a></button>
{{/if}}
<a href="{{page}}"></a>
{{#if hasNextPage}}
    <button><a href="{{nextLink}}">NEXT</a></button>
{{/if}}

<script src="/socket.io/socket.io.js"></script>
<script src="../public/cart.js"></script>

